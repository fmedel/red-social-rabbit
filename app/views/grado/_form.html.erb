<%= simple_form_for(@grado, url: grados_guardar_ok_path(), html: {class: "form-inline"}) do |f| %>
  <% if @grado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grado.errors.count, "error") %> prohibited this grado from being saved:</h2>

      <ul>
      <% @grado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group has-success has-feedback">
  <div class="field">
    <%= f.input_field :descripcion ,placeholder: 'aqui va la descripcion que quiere dar '%>
  </div>
  <div class="form-actions">
    <%= f.button :submit  ,'Guardar ', url: '?hola'%>
   
  </div>
  </div>
<% end %>
