<div class="formulario"> 
  <%= simple_form_for(@estado, url: ver_estado_path(@estado), html: {class: "form-inline"}) do |f| %>
    <% if @estado.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@estado.errors.count, "error") %> prohibited this estado from being saved:</h2>

        <ul>
        <% @estado.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <div class="form-group has-success has-feedback">
    <div class="field">
      <%= f.input_field :descripcion ,placeholder: 'nombre'%>
    </div>
   
    <div class="form-actions">
      <%= f.button :submit  ,'Guardar '%>
    </div>
    </div>
  <% end %>
</div>