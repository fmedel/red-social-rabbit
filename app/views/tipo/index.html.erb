 
<div class="page-header">
  <h1>Todas los tipos disponibles </h1>
</div>

<div class="resultados_tipo">
<% @tipo.each do |tipo| %>
  <div class="tipo">
    <hr>
    <h4>
      <strong>
        <%=tipo.id%>:<%= tipo.descripcion %>
      </strong>
        (
        <%= link_to 'Editar', editar_tipo_path(tipo.id) %> 
        | 
        <%= link_to 'Eliminar', eliminar_tipo_path(tipo.id), method: :delete, data: { confirm: '¿Estas seguro?' } %>
        )
    </h4>
    <hr>
  </div>
<% end %>
</div>
<br>
<%= link_to 'crear nuevo estado', tipo_guardar_path %>

